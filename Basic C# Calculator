'm a complete beginner ar Programming and today I decided to test what I've learned So far and make a basic C# Calculator. I've finished it, but now I'm looking for ways I can Clean it up. It will help me be cleaner and make me learn. If you have any suggestions, Please Share! Thanks!

using System;

namespace lol
{
    class Program
    {
        static void Main(string[] args)
        {
            Console.WriteLine("Hi! What is your name?");
            string name = Console.ReadLine();

            Console.WriteLine(name + " What do you wanna do?");

            Console.WriteLine("Type \"+\" for addition");
            Console.WriteLine("Type \"-\" for addition");
            Console.WriteLine("Type \"*\" for addition");
            Console.WriteLine("Type \"/\" for addition");

            string operation = Console.ReadLine();

            Console.Write("Now, Give me number one: ");
            double num1 = Convert.ToDouble(Console.ReadLine());

            Console.Write("Now give me number two: ");
            double num2 = Convert.ToDouble(Console.ReadLine());

            if (operation == "+")
            {
                Console.WriteLine(num1 + num2);
            }
            else if (operation == "-")
            {
                Console.WriteLine(num1 - num2);
            }

            else if (operation == "*")
            {
                Console.WriteLine(num1 * num2);
            }

            else
            {
                Console.WriteLine(num1 / num2);

            }

        }
    }
}
